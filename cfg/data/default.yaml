defaults:
    - init: default
    - input: ${model}
    - output: ${model}


path:    
    directory: /libre/dumontj/coco2/dl-input
    train:
        name: all_hotspots_but_lip
        nc: train_dataset.nc
    valid: 
        name: ${data.path.train.name}
        nc: valid_dataset.nc

